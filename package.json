{
  "name": "ffmpeg-cli-gen",
  "version": "0.0.1",
  "description": "An ffmpeg commandline configurator",
  "keywords": "ffmpeg",
  "license": "GTK",
  "author": "Alex",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
